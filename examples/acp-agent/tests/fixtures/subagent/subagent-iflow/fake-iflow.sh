#!/bin/sh
# Test-only fake iFlow CLI for the loader-composition smoke: prints a canned
# final answer on stdout and exits 0, exactly like a successful non-interactive
# `iflow -p` run (which prints its final answer on stdout).
echo "fake iflow answer"
